name = "dashgen"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"

[vars]
NEXT_PUBLIC_APP_NAME = "DashGen"
FREE_GENERATION_LIMIT = "3"

# AI vars (AI_PROVIDER, AI_MODEL, ANTHROPIC_API_KEY, ANTHROPIC_BASE_URL)
# are set via Cloudflare Pages Environment Variables, NOT here
